# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

# Dependencies
node_modules

# Build outputs
dist
dist-ssr
*.local

# Ignorar artefactos de builds y pruebas
artifacts/
bun.lockb
.vite/

# Environment files (IMPORTANTE: nunca commitear con valores reales)
.env
.env.local
.env.*.local
.env.development
.env.staging
.env.production
.env.vercel

# Recomendación: nunca subas tus claves reales a GitHub

# TypeScript
*.tsbuildinfo

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Server data
server/data/*.db
server/data/*.db-shm
server/data/*.db-wal
server/uploads/*
!server/uploads/.gitkeep

# Docker volumes
postgres_data/
pgadmin_data/
prometheus_data/
grafana_data/

# Backups
*.sql
*.dump
backup-*.tar.gz

# Environment variables
.env
.env.local
# .env.production  # Comentado: necesario para Lovable
!.env.example
!.env.local.example

# Supabase
supabase/.temp/
supabase/.branches/

# Package manager locks (solo npm)
bun.lockb
yarn.lock
pnpm-lock.yaml

# Server data and uploads
server/data/*.db
server/data/*.db-*
server/uploads/*
!server/uploads/.gitkeep

# Docker volumes
postgres_data/
pgadmin_data/
prometheus_data/
grafana_data/

# Environment files
.env
.env.local
# .env.production  # Comentado: necesario para Lovable
.env.*.local

# Backups
*.sql
*.dump
backup-*

.vercel
